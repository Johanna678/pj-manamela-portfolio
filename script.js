const menu=document.querySelector('.menu'),nav=document.querySelector('nav');if(menu)menu.addEventListener('click',()=>nav.classList.toggle('open'));
const reveal=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.08});document.querySelectorAll('.section,.stats').forEach(x=>{x.classList.add('reveal');reveal.observe(x)});
const form=document.getElementById('contact-form'),status=document.getElementById('form-status');
if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));const name=data.name?.trim()||'';const email=data.email?.trim()||'';const message=data.message?.trim()||'';if(!name||!email||!message){status.textContent='Please complete all fields.';return;}status.textContent='Message ready to send. Please email me directly.';form.reset();});}
window.addEventListener('DOMContentLoaded',()=>{const subtitle=document.querySelector('.hero-text');if(subtitle){const words=['Software Developer','Java Developer','Web Developer','Android Developer','Cloud Computing Enthusiast','Database Developer/Administrator','Data Scientist','System Analyst','Quality Engineer','Mobile Solutions Developer','UI/UX Designer'];let wordIndex=0;let charIndex=0;let deleting=false;function type(){const current=words[wordIndex];subtitle.textContent=current.substring(0,charIndex);if(!deleting){charIndex++;if(charIndex>current.length){deleting=true;setTimeout(type,1500);return;}}else{charIndex--;if(charIndex===0){deleting=false;wordIndex++;if(wordIndex>=words.length)wordIndex=0;}}setTimeout(type,deleting?60:120);}type();}});
